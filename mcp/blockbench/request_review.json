{"name":"request_review","description":"SHOW THE USER and WAIT for their verdict — the honest alternative to looking at your own screenshot and declaring it good. Renders labelled views (or animation poses) and posts them as a card in the MCP Copilot panel inside Blockbench, then waits a SHORT window for the user to press 'Looks right' / 'Needs changes' and optionally type a comment. If they haven't answered yet it returns `pending: true` with a `review_id` — the card stays open, so call wait_review with that id to keep waiting (that loop is how a minutes-long human review fits inside an MCP client's per-request timeout). Call it after every user-visible milestone: the blockout, the texture pass, EACH animation, anything the user asked for specifically, and before you claim a task is finished. Run the objective checks (check_model / check_sides / check_rig / analyze_animation) FIRST — don't spend the user's attention on something a tool would have caught. If the answer is 'needs changes', fix exactly what they said and ask again. `pending` and a timeout are NOT approval.","parameters":{"properties":{"animation":{"description":"Animation name/uuid to review — renders poses from it instead of the rest pose.","type":"string"},"details":{"description":"What you changed since the last review, and anything you are unsure about.","type":"string"},"options":{"description":"Custom answer buttons instead of approve/reject (e.g. ['Left hand','Right hand']).","items":{"type":"string"},"type":"array"},"question":{"description":"One concrete question, naming what to look at. e.g. 'Walk cycle: is the stride right and do the knees bend enough?'","type":"string"},"timeout_seconds":{"description":"How long the card stays open in Blockbench (default 900, max 3600).","type":"number"},"times":{"description":"Times (seconds) to sample when `animation` is set. Default 0/25/50/75%.","items":{"type":"number"},"type":"array"},"title":{"description":"Short header for the panel card.","type":"string"},"views":{"description":"Camera views to render, named from the MODEL's point of view: 'front' (its face), 'back', 'left'/'right' (the side its left/right arm is on), 'front_right', 'top'. Default front_right/front/left/back.","items":{"type":"string"},"type":"array"},"wait_seconds":{"description":"How long THIS call waits before returning `pending` (default 25). Keep it under your client's request timeout; use wait_review to keep waiting.","type":"number"}},"required":["question"],"type":"object"}}