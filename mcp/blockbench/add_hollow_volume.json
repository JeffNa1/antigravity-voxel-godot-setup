{"name":"add_hollow_volume","description":"Build a SHELL instead of a solid box — the fix for the single biggest 'AI model' tell, a monolithic cube where a hollow form belongs. Creates up to six walls of `wall_thickness` around an empty cavity, skipping every direction in `open_faces`. Universal: hoods (open 'north'+'down' so the face and neck show), helmets, masks, visors, eye sockets, breastplates, pauldrons, bracers, collars, cages, wheels, pipes, chimneys, crates, troughs. Faces are WORLD-relative — north=-Z (the model's front), south=+Z, east=+X, west=-X, up, down — and the model-relative words front/back/left/right/top/bottom are accepted too. The walls tile the shell exactly, so they never overlap or z-fight each other; anything you place INSIDE the cavity should still keep \u003e=0.1 clearance from them. Returns the cavity bounds so you can fill it.","parameters":{"properties":{"bounds":{"description":"OUTER box of the shell: {from:[x,y,z], to:[x,y,z]}.","properties":{"from":{"description":"Lower corner.","items":{"type":"number"},"maxItems":3,"minItems":3,"type":"array"},"to":{"description":"Upper corner.","items":{"type":"number"},"maxItems":3,"minItems":3,"type":"array"}},"required":["from","to"],"type":"object"},"inflate":{"description":"Inflate every wall (rounds the shell without moving it).","type":"number"},"name":{"description":"Base name; walls are named \u003cname\u003e_north, \u003cname\u003e_up, ... (default 'shell').","type":"string"},"open_faces":{"description":"Directions left open, e.g. ['north','down'] for a hood or ['up'] for an open crate.","items":{"enum":["north","south","east","west","up","down","front","back","left","right","top","bottom"],"type":"string"},"type":"array"},"parent":{"description":"uuid or name of the bone to build into.","type":"string"},"side":{"description":"The model's own side, enforced against the geometry (e.g. a single pauldron).","enum":["left","right"],"type":"string"},"wall_thickness":{"description":"Wall thickness in units (default 1; 1.5-2 reads as heavy armour). Clamped per axis if it would not fit.","type":"number"}},"required":["bounds"],"type":"object"}}