{"name":"ask_user","description":"Ask the user a question and wait for the answer, without ending your turn — use it instead of guessing when a decision is genuinely theirs (which hand holds the shield, which of two silhouettes, which palette). The question appears as a card in the MCP Copilot panel inside Blockbench; pass `options` for one-click choices, and optionally `views` to show renders alongside it. Like request_review it waits a short window and may return `pending: true` with a `review_id` — then keep polling with wait_review.","parameters":{"properties":{"details":{"description":"Extra context or the trade-offs between the options.","type":"string"},"options":{"description":"Answer buttons.","items":{"type":"string"},"type":"array"},"question":{"description":"The question, phrased so it can be answered in one click or one line.","type":"string"},"timeout_seconds":{"description":"How long the card stays open in Blockbench (default 900).","type":"number"},"title":{"type":"string"},"views":{"description":"Optional model views to show with the question.","items":{"type":"string"},"type":"array"},"wait_seconds":{"description":"How long THIS call waits before returning `pending` (default 25).","type":"number"}},"required":["question"],"type":"object"}}