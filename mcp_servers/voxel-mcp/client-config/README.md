Claude Desktop Configuration
----------------------------

1) Build the server in `voxel-mcp/`:
   - `npm install`
   - `npm run build`

2) Open Claude Desktop → Settings → Developer → Model Context Protocol → Edit config.

3) Paste JSON from `claude-desktop.json` and adjust `cwd` if your path differs.

4) Save. Claude will launch the MCP server when needed.

